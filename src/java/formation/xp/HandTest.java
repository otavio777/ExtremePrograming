
import org.junit.Test;

import junit.framework.TestCase;

public class HandTest extends TestCase{
	public void init_hand_Test(){
		Hand h=new Hand();
		Paquet p=new Paquet();
		//h.init_hand(p);
		//if (h.getNumber()==2 ) System.out.println("HandTest verifié");//&& (((h.getCards()).get(0).getMyValue())!=(h.cards).get(1).mySuit || (h.getCards()).get(0).myValue!=(h.getCards()).get(1).myValue)
	}
}
